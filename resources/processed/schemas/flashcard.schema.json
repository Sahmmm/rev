{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.local/rev/flashcard.schema.json",
  "title": "Flashcard",
  "type": "object",
  "required": [
    "id",
    "type",
    "question",
    "answer",
    "difficulty",
    "topicIds",
    "tags",
    "source",
    "status"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^fc-[a-z0-9-]+$"
    },
    "type": {
      "const": "flashcard"
    },
    "question": {
      "type": "string",
      "minLength": 1
    },
    "answer": {
      "type": "string",
      "minLength": 1
    },
    "difficulty": {
      "type": "string",
      "enum": ["easy", "medium", "hard"]
    },
    "topicIds": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9-]+$"
      },
      "uniqueItems": true
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9-]+$"
      },
      "uniqueItems": true
    },
    "source": {
      "$ref": "#/$defs/source"
    },
    "hints": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "commonMistakes": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "status": {
      "type": "string",
      "enum": ["draft", "reviewed", "active", "archived"]
    }
  },
  "$defs": {
    "source": {
      "type": "object",
      "required": ["resourceId", "resourceType", "file"],
      "additionalProperties": false,
      "properties": {
        "resourceId": {
          "type": "string",
          "pattern": "^[a-z0-9-]+$"
        },
        "resourceType": {
          "type": "string",
          "enum": ["course", "exam", "tp", "personal-note"]
        },
        "file": {
          "type": "string",
          "minLength": 1
        },
        "locator": {
          "type": ["string", "null"]
        }
      }
    }
  }
}
